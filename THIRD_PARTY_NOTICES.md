# Third-party notices

Orbital Team Workspace 的运行时直接依赖由 `pyproject.toml` 锁定：

| Package | Version range | License |
|---|---:|---|
| `filelock` | `>=3,<4` | The Unlicense |
| `jsonschema` | `>=4,<5` | MIT |

测试工具 `pytest` 不属于 runtime dependency。各依赖的完整许可证文本与版本信息以安装包及其
上游发行页面为准。本文件只做 attribution，不改变任何第三方许可证，也不为本作业 repo
授予许可证。

